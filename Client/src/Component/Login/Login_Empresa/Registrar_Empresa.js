import './Registrar_Empresa.css'
const RegistrarEmpresa = ({setView}) => {
    return (

        <dev>

            Registrar Usuario
            <dev className="btn-regresar" onClick={() => setView("login")}>Regresar</dev>
        </dev>
    )
}
export default RegistrarEmpresa;